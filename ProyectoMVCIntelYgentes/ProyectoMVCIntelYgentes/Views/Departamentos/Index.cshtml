@model IEnumerable<ProyectoMVCIntelYgentes.Models.Datos.Departamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DeptoNombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DeptoNombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeptoNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeptoNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeptoNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
